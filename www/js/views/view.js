App.ViewTitleHeader = Ember.View.create({
	  templateName: 'title-header',
	  title: null
});

App.ViewHeader = Ember.View.create({
	  templateName: 'header'
});

App.ViewAdd = Ember.View.create({
	  templateName: 'add'
});